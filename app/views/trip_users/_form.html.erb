<%= form_with(model: trip_user) do |form| %>
  <% if trip_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trip_user.errors.count, "error") %> prohibited this trip_user from being saved:</h2>

      <ul>
        <% trip_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :trip_id, style: "display: block" %>
    <%= form.text_field :trip_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
