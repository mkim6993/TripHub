test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use bin/rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
amazon:
  service: S3
  access_key_id: AKIA2UUQKPGKGCLAWHUM
  secret_access_key: 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
  region: us-east-1
  bucket: triphubstorage
