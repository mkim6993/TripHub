test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use bin/rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
amazon:
  service: S3
  access_key_id: AKIA5BCYCD2747FV2PI2
  secret_access_key: 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
  region: us-east-1
  bucket: triphubstorage
